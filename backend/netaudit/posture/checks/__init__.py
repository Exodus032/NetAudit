"""Check implementations, one module per Part A category. Every module is
imported for its `@register` side effects by `..registry._ensure_loaded()` --
nothing in here is imported directly by name from outside the package."""
